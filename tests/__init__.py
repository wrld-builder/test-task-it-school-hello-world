"""
Test package for the hero API.

This file is intentionally left empty but exists so that Python treats
the ``tests`` directory as a package.  Without it, pytest may not
discover tests inside this folder when running within the Django
project context.
"""